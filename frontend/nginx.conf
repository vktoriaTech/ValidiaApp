server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: cualquier ruta que no sea un archivo real cae a index.html
    # (necesario porque el frontend usa react-router-dom)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
